apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
spec:
  interval: 30m0s
  prune: true
  sourceRef:
    kind: ExternalArtifact
    name: infrastructure
  path: ./
  decryption:
    provider: sops
    secretRef:
      name: sops-age
