apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: filestash
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - "files.k8s.nyumbani.home"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: filestash
          port: 80
