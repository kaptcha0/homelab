apiVersion: v1
kind: Service
metadata:
  name: filestash
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: filestash
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: filestash-internal
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: filestash
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
