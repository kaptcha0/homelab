apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
spec:
  interval: 10m
  chart:
    spec:
      chart: traefik
      version: "38.0.x"
      sourceRef:
        kind: HelmRepository
        name: traefik
      interval: 10m
  valuesFrom:
    - kind: ConfigMap
      name: traefik-values
